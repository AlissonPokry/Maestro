Read and follow `maestro/SKILL.md`, section `/maestro`. Route the user task through Maestro.

When reporting what Maestro used, bundle names are not skill names. List skill file paths from selected_bundles[].skill_paths[].path as skills, and list selected_bundles[].bundle separately as bundles.

