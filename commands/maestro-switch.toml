---
description = "Change Maestro's saved skill-bundle-folder"
prompt = "Use Maestro from ./maestro/SKILL.md. Run the /maestro-switch workflow for {{args}}. If {{args}} is empty, ask for the new skill-bundle-folder path. Save it with `python ./maestro/scripts/maestro_config.py set \"<path>\" --skill-file ./maestro/SKILL.md`, then refresh with `python ./maestro/scripts/maestro_fetch.py --skill-file ./maestro/SKILL.md`."
---
