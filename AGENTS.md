When the user invokes `/maestro`, `/maestro-fetch`, `/maestro-set`, or `/maestro-stats`, you MUST read and follow the instructions in `maestro/SKILL.md`.

Use `maestro/SKILL.md` as the absolute source of truth for routing tasks and managing skill bundles. Do not duplicate its routing logic.
