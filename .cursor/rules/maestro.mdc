---
description: "Maestro routes /maestro tasks to project skill bundles, refreshes bundle keywords with /maestro-fetch, and changes bundle path with /maestro-switch"
alwaysApply: true
---

When user invokes `/maestro`, `/maestro-fetch`, `/maestro-switch`, or `/maestro-stats`, read and follow `maestro/SKILL.md`.

Use `maestro/SKILL.md` as source of truth. Do not duplicate bundle-routing behavior here.

